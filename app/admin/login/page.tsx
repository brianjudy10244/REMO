"use client";
import { useState } from "react";
export default function LoginPage(){ const [error,setError]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(r.ok)location.href="/admin";else setError("관리자 ID 또는 비밀번호를 확인해주세요.");} return <main className="login"><form onSubmit={submit}><a className="wordmark" href="/">리모<span className="star">✳</span></a><small>REMO STUDIO</small><h1>관리자 로그인</h1><label>관리자 ID<input name="username" defaultValue="remo_admin" autoComplete="username"/></label><label>비밀번호<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<p className="error">{error}</p>}<button className="admin-button">로그인</button><a href="/">← 홈페이지로</a></form></main> }
